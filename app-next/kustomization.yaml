apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/instance: prod
      app.kubernetes.io/name: milestone-timeline-next

nameSuffix: -next

resources:
  - ./deployment.yaml
  - ./service.yaml
